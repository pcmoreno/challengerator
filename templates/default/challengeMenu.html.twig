<table>
        <tr><td>challenge</td><td></td></tr>
    {% for item in challenges %}
        <tr><td>{{ item }}</td>
                <td>
                        <button><a href = {{ path('loginMenu', {
                                        'challengeName': item
                                }) }}>GO TO</a></button>
                </td>
        </tr>
    {% endfor %}
</table>

{% include 'default/generalStyle.html.twig' %}
