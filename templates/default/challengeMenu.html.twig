<table>
        <tr><td>challenge</td><td></td></tr>
    {% for item in challenges %}
        <tr><td>{{ item }}</td>
                <td>
                        <button><a href = {{ path('loginMenu', {
                                        'challengeName': item
                                }) }}>GO TO</a></button>
                </td>
        </tr>
    {% endfor %}
</table>
{% if challeges is empty %}
<div style="text-align: center;"><h1 style="font-family: 'Arial Narrow',cursive">no challenges available at the moment</h1></div>
{% endif %}
{% include 'default/generalStyle.html.twig' %}
