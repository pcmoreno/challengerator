<h1>{{ challengeName }}</h1><br>

{{ form_start(form) }}
    {{ form_row(form.user) }}
    {{ form_row(form.pass) }}
    {{ form_widget(form.login) }}
{{ form_end(form) }}

{% include 'default/generalStyle.html.twig' %}

<style>
    form {
        margin: auto;
        width: 50%;
    }
</style>
